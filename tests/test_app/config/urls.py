"""
Sets the mapping between `url_endpoints` and `view functions`.
"""
#from lib.utils import set_trace

routes = [
    # Define non-blueprint routes here. Blueprint routes should be in
    # a separate urls.py inside the blueprint package.
]
